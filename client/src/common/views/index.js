const {ColumnSelect} = require("./ColumnSelect");
const {InputForm} = require("./InputForm");
const {TableSelect} = require("./TableSelect");
const {TableView} = require("./TableView");

module.exports = {
    ColumnSelect,
    InputForm,
    TableView,
    TableSelect
}